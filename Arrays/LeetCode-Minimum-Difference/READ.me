# Minimum Difference Between Highest and Lowest of K Scores

## 📌 Problem
Given an integer array `nums` and an integer `k`, pick `k` elements such that the difference between the maximum and minimum elements is minimized.

🔗 LeetCode Problem: Minimum Difference Between Highest and Lowest of K Scores

---

## 🧠 Approach
1. If `k == 1`, the minimum difference is `0`.
2. Sort the array.
3. Use a sliding window of size `k`.
4. Calculate the difference between the first and last element of each window.
5. Track the minimum difference.

---

## ✅ Example
**Input**
